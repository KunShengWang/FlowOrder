server:
  port: 8088

spring:
  application:
    name: floworder-gateway-service
  profiles:
    # 微服务调用
    active: pro
  # nacos
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
