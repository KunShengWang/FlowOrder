讲下v3版本下单的流程：
1、在初始接收到请求之后会进行库存的预扣，先把库存锁定，然后同时把扣减记录和消息记录分别存入数据库中。
2、RabbitMQ进行异步的消费。首先resource-service会定时的查找可发送的订单创建消息然后发给RabbitMQ，同时同步等待 Broker 的 Confirm 确认和 Return 回调用于更新fo_mq_outbox表的状态。
然后order-service对应的消费队列会接收到对应交换机发送的订单创建消息进行消费，消费RabbitMQ消息的时候会在fo_mq_consume_log表中留下消费记录。
order-service创建好订单之后会封装一个OrderCreateResultMessage实体类然后存入fo_mq_outbox表，然后order-service侧的定时任务会定时的把订单创建结果消息发送到指定队列。
然后resource-service侧的订单结果消费队列会消费这个订单创建结果消息，然后会更新fo_stock_deduct_record表中的状态，同时也会在fo_mq_consume_log表中留下消费记录。
3、order-service侧可以通过接口确认和取消订单，还可以通过定时任务去判断订单是否超时。这三种情况都会在fo_order_status_log表中留下日志并把订单状态连同OrderStateChangedMessage存入fo_mq_outbox表。
然后order-service的定时任务会把订单状态的消息发送到指定的交换机，然后resource-service侧的订单状态消费队列会消费此消息，会根据状态来判断是库存是锁定到sold_stock还是释放到available_stock。
4、死信落库-MqDeadLetterConsumer消费三条DLQ，调用deadLetterService.record(...);把记录插入到fo_mq_dead_letter表中，如果是创建订单失败的死信，即订单创建命令死信表示创建结果未知，不代表订单明确创建失败，因此需要把fo_stock_deduct_record表中的状态从PRE_DEDUCTED改为MANUAL_REVIEW。
5、人工重放MqDeadLetterAdminController(需要先查到fo_mq_dead_letter表中的数据)-如果是订单创建死信队列需要更新fo_mq_dead_letter表的状态PENDING -> REPLAYING；把fo_stock_deduct_record表的状态改为MANUAL_REVIEW -> PRE_DEDUCTED；还需要把fo_mq_outbox表的状态改为SENT/DEAD -> RETRY。
如果是订单结果队列或者订单状态队列只需要更新fo_mq_dead_letter表的状态PENDING -> REPLAYING。
然后需要远程调用order-service的接口去把fo_mq_outbox表中订单结果和订单状态的状态改为STATUS_RETRY。
解释下：因为订单创建是resource-service的定时任务去查fo_mq_outbox表的消息然后发送给RabbitMQ；订单结果和订单状态是order-service的定时任务去查fo_mq_outbox表的消息然后发送给RabbitMQ。所以上述第五点的步骤不同。
6、由MqDeadLetterMonitorTask对fo_mq_dead_letter表进行后续处理，deadLetterService.recoverStaleReplaying(...)，如果fo_mq_dead_letter表被正确处理，就把状态改为已解决；如果没有被正确处理就需要把状态从重放中改为待处理。如果是订单创建死信队列还需要额外把fo_stock_deduct_record表中的状态从预扣改为人工确认。
7、如果OrderResultConsumer和OrderStateConsumer在重试期间把异常信息处理好了，就可以更新fo_mq_dead_letter表的状态从待处理\重放中改为已解决。

简单来说：
1、死信落库闭环：
 - 首先MqDeadLetterConsumer先把死信队列中的消息落库(fo_mq_dead_letter)
 - 然后人工确认消息(MqDeadLetterAdminController)，创建订单、订单结果、订单状态三种操作都会更改fo_mq_dead_letter、fo_mq_outbox表的状态，然后创建订单会额外修改fo_stock_deduct_record表的状态。
 - 然后流程就跟之前的流程一样，定时任务查fo_mq_outbox表的消息然后发给RabbitMQ进行消费。
 - 然后MqDeadLetterMonitorTask会定时的检查死信消息处理的情况，如果没有正确处理会更改fo_mq_dead_letter表的数据的状态，等待下一次处理；如果处理成功就会把状态改为已解决。
 - 如果OrderResultConsumer和OrderStateConsumer在重试期间把异常信息处理好了，就可以更新fo_mq_dead_letter表的状态从待处理\重放中改为已解决。